//////// template String
// var info = {
//   name: "yaman",
//   age: 27,
//   job: "devloper",
// };
// var UserInfo = `user${info.name}, age:${info.age}`;
// console.log(UserInfo);

//////////// loops
let jobs = ["developer", "admin", "tester", "ca"];
for (var job of jobs) {
  console.log(`jobs title: ${job}`);
}
